apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-auth
  namespace: {{ .Values.dashboard.namespace }}
spec:
  basicAuth:
    secret: dashboard-auth-secret
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-auth
  namespace: {{ .Values.dashboard.namespace }}
spec:
  basicAuth:
    secret: dashboard-auth-basic
