apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.basicAuth.secretName }}
  namespace: {{ .Values.dashboard.namespace }}
type: Opaque
data:
  users: {{- range $i, $u := .Values.basicAuth.users -}}
    {{ $u | b64enc }}
    {{- end -}}
